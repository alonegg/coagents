# 安全问题报告

请不要在公开 Issue 中披露漏洞。使用 GitHub 的 **Report a vulnerability**（本仓库 Security 页面的私密报告）提交，说明影响范围、复现步骤和受影响版本。我们会尽快确认并在修复发布后致谢（如你愿意）。

适用范围：团队服务（`apps/server`）、Hub（`apps/hub`）、Connector（npm 包 `coagents`）与部署脚本（`deploy/`）。
