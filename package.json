{
  "name": "coagents",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": { "node": ">=22.12" },
  "scripts": {
    "check": "tsc -b && pnpm -F @coagents/hub check",
    "test": "vitest run",
    "build": "tsc -b && pnpm -F @coagents/hub build"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
