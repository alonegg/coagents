{
  "name": "coagents-monorepo",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "check": "tsc -b && tsc -p tests && pnpm -F @coagents/hub check",
    "test": "vitest run",
    "build": "tsc -b && pnpm -F @coagents/hub build && pnpm -F coagents bundle"
  },
  "devDependencies": {
    "@hono/node-server": "^2.1.1",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@types/node": "^22.19.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
