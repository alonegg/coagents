# CoAgents 第一版实施计划

版本：0.3 · 日期：2026-09-23 · 状态：M0–M8 已通过；M9 除 LAN 路径外均已完成，见 [验收记录](../docs/records/acceptance.md)。

第一版以多人分布式协作为主线，21 项 FR 全部是发布门槛。开发顺序是先打通一条跨机最小闭环再加宽，而不是按层完成基础设施后再做业务。Hub 从 M1 起就提供最小可用版本，作为人工验收入口逐步补全。任何里程碑都以下列 done criteria 的实测结果为准，"代码写完"不算完成。

业务依据 [PRD](../docs/PRD.md) 与 [Use Cases](../docs/USE_CASES.md)；技术选型见 [架构草案](../docs/ARCHITECTURE.md)；主机见 [部署环境](../docs/DEPLOYMENT.md)。

| 里程碑 | 内容 | Done criteria | 状态 |
| --- | --- | --- | --- |
| M0 决策与骨架 | 技术栈与 OD-01/02/04 决定；修正文档不一致；git 仓库；monorepo（contract、server、hub、connector）；CI | `pnpm install && pnpm check && pnpm test` 本地通过；CI 工作流同样通过；契约 schema 只在 `packages/contract` 定义 | 已通过（2026-09-23：本地与 GitHub Actions CI 均通过） |
| M1 跨机登录 | SQLite 迁移、CLI 首次设置、用户名密码登录、邀请注册、项目与成员、最小 Hub；部署到主机并配置 HTTPS | 两台独立机器经 HTTPS 登录看到同一项目；无邀请注册、证书错误、越权访问均被拒绝（UC-01、UC-12、UC-17 主路径） | 已通过（2026-09-23）：机器 A 为开发机，机器 B 为 GitHub 托管 runner（美国网络），经 `https://coagents.chengdu80.org` 完成邀请注册、接受、隔离与拒绝检查，见 `scripts/e2e/m1.mjs`；服务端 27 项测试。客户端证书校验留到 M3 在 Connector 上验证 |
| M2 任务核心 | 任务、原子租约、语义状态动作、版本冲突、幂等、事件序号、决策/阻塞；表驱动权限测试 | 两台机器各发 100 次并发认领，双有效持有者为 0；丢弃响应后重试不产生重复写入（UC-04、UC-05、UC-06） | 已通过（2026-09-23）：开发机与 GitHub runner 在约定时刻同时认领 100 个任务，A 胜 22、B 胜 78、双方均成功 0，服务端持有者与胜者逐一一致；中断首个请求后以同一 request_id 重试两次，只有 1 个任务。见 `scripts/e2e/m2.mjs`；服务端 45 项测试 |
| M3 Connector + Claude Code | stdio MCP、设备码授权、`.coagents/project.json` 绑定、配置预览/安装/卸载 | Claude Code 真实完成 `get_context`、`claim_task`、`submit_task`；卸载后配置与安装前逐字一致（UC-13） | 已通过（2026-09-23）：见 [Claude Code 集成记录](../docs/records/claude-code.md)；设备码授权经 Hub 页面批准；服务端与 Connector 共 59 项测试 |
| M4 实时与撤权 | SSE、游标补读、通知去重、送达/已读分开、撤权主动断开连接 | 断网重连后不漏事件；撤销设备后旧推送连接不再收到事件，其他设备正常（UC-15、UC-18、UC-22） | 已通过（2026-09-23）：跨机 ping/pong 30 次，A→B→A 往返 p95 854ms（单程约 0.43s）；B 断流期间 A 写 20 个任务，B 按游标续传收到 22 条事件且与服务端列表逐条一致；B 在线撤销第二台设备，其旧流约 1s 内收到 revoked 并关闭、此后无事件、API 401，第一台设备继续实时；服务重启后流自动重连，重启后写入 1.45s 内送达。见 `scripts/e2e/m4.mjs`；63 项自动测试 |
| M5 成果与验收 | 草稿/发布版本、上传、受限名单、Markdown 清理、提交/接受/退回、审计 | UC-07 至 UC-11 通过；直链下载受限文件被拒绝 | 已通过（2026-09-23）：A 发布仅限贡献者的预算文件，B 上贡献者下载校验一致，B 上查看者在列表、事件、详情均无痕迹、直链下载 404；导入报告保留原作者与日期、草稿期对查看者不可见；提交绑定 v1，A 人工接受后发布 v2 绑定不变；浏览器实测 Markdown 中 script、onerror、javascript: 链接均被清除。见 `scripts/e2e/m5.mjs`（首轮因脚本自身在重试时留下未受限副本而失败，修正后重跑通过）；74 项自动测试 |
| M6 交接 + Codex CLI | Git 只读检查、交接状态机、Codex CLI 适配 | 接收端缺 commit 时拒绝接手，补齐后接手成功（UC-14、UC-19）；两种客户端都有实测记录 | 已通过（2026-09-23）：开发机上真实 Codex CLI 认领并定向交接，GitHub runner 上 Connector 因缺 commit 被拒且工作副本不变，fetch 后接手并提交；见 [Codex 记录](../docs/records/codex.md) 与 [Claude Code 记录](../docs/records/claude-code.md)；84 项自动测试（含真实 git 的交接集成测试） |
| M7 里程碑与检索 | 里程碑/截止日期/时区；中文分词方案验证后实现全文索引（MD/TXT/PDF 文本层）与检索期权限过滤 | 仅在正文出现的中文词可检索；撤权后片段与总数立即不再返回（UC-20、UC-21） | 已通过（2026-09-23）：B（账户时区洛杉矶、runner UTC）读到的里程碑截止时刻与 A（项目时区上海）同为 2026-10-01T15:59:59.999Z，数量与看板一致；Chrome 生成的中文 PDF 以正文独有词命中第 2 页；A 限制备忘后 B 检索总数 0 且无片段；有未完成任务时确认达成被拒，带原因移出后确认成功。准备验收时发现 PDF 抽取出康熙部首兼容字符导致漏检，已改为索引文本 NFKC 规范化并加回归测试。见 `scripts/e2e/m7.mjs`；96 项自动测试 |
| M8 Hub 补全与归档 | 跨项目活动、概览统计、归档/恢复、审计页 | UC-02、UC-03、UC-16 通过 | 已通过（2026-09-23）：B 的卡片与跨项目活动只含共享项目；A 归档后 B 写入返回 project_archived、读取正常、项目进入归档筛选，恢复后可写；A 删除项目后 B 的直链、卡片与活动均不再出现；审计含归档与恢复。见 `scripts/e2e/m8.mjs`；100 项自动测试 |
| M9 团队验收 | 三账户、两台机器、两种客户端；公网与 LAN 路径；故障注入；按 PRD 容量压测；发布扫描 | 22 个用例及失败路径均有实测记录；[命名与发布检查](../docs/RELEASE_HYGIENE.md) 扫描零命中 | 部分通过（2026-09-23）：22 个用例中 21 个通过，UC-17 的 LAN 路径未验证；故障注入五项、PRD 容量（常规读取 p95 ≤ 74 ms）、全新机器安装卸载、备份恢复、命名扫描均通过。见 [验收记录](../docs/records/acceptance.md) |

## 发布关口

- 远程接入、实时通知、跨设备 Git 交接、里程碑/截止日期、全文检索任一缺失，都阻止第一版发布。
- 交付服务部署说明、客户端接入与卸载流程、身份恢复和备份/恢复说明。
- 未达门槛的功能保留"未通过"状态，不以文档覆盖代替实测。
